class Solution {
    /**
     * @param {number[]} nums
     * @param {number} target
     * @return {number}
     */
    search(nums, target) {
        //console.log(nums.indexOf(target))
        return nums.indexOf(target)
    }
}
